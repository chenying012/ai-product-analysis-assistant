# AI 产品分析助手：需求理解与参考项目调研

调研日期：2026-09-14

题目依据：工作区中的 `AI工程师实习生笔试题.pdf`、`要求.md`

范围：公开资料检索、5 个参考仓库的固定快照与核心源码审阅；**未安装或运行参考项目，未调用商品采集/模型 API，未实测 Amazon 链接成功率**。

## 1. 结论先行

这道题不是“做一个爬虫”，也不是“给模型一个链接让它写广告”。真正的交付是：

**指定商品链接 → 可信的商品信息 → 有依据的产品理解 → 能直接用于短视频的中文口播 → 可公开访问并接受陌生链接测试的网页。**

本次审阅范围内，没有发现可以不加改造、直接完整满足题目的项目。比较合适的策略是**自己写一个薄的业务应用，借鉴成熟模块，而不是整体 fork 某个大型项目**。

优先级建议：

1. **先验证商品信息来源，再精修页面。** 采集不到指定商品的有效内容，后面的模型与 UI 都无法补救。
2. **结构化输出与事实核验分开。** 符合 JSON Schema，只说明格式合规，不说明价格、功能或卖点真实。
3. **最有价值的加分项是证据约束与质量检查。** 先让事实、推断、缺失信息可区分，再考虑图片理解。
4. **推荐轻量路线：Next.js + TypeScript + 服务端采集适配器 + 一次结构化分析/文案生成 + 确定性校验。** 这是调研建议，不是已经确定或实现的技术栈；若开发者更熟悉 Python，Streamlit 也符合题目。
5. **不默认引入数据库、向量检索、多智能体、追价任务或视频生成。** 当前输入是单个商品，输出是一次分析，原题没有要求这些能力。

仓库初始化已完成：`README.md` 为 0 字节，首次提交 `28636b8` 已正常推送并核对远端一致。本调研文档与参考源码没有随之提交或推送。

## 2. 对题目的深度理解

### 2.1 必做、加分与自主设计必须分清

| 层级 | 原题要求 | 我们应采用的验收解释 |
| --- | --- | --- |
| 必做 | 可运行的网页小工具 | 不只交脚本或 Notebook；用户能输入链接、看到状态和结果 |
| 必做 | 输入 Amazon 商品链接 | 分析用户指定的商品，不替换成关键词搜索结果、相似商品或固定样例 |
| 必做 | 产品信息整理 | 名称、品类、价格、功能、规格等按可获得信息整理；“例如”不是要求缺失字段也必须填满 |
| 必做 | 产品分析 | 解释功能对谁、在什么情境、解决什么问题有价值，不能只是换句话复述详情 |
| 必做 | 中文短视频口播，150 字以内 | 钩子与正文合计受限，不是 150 词、150 token 或只限制正文 |
| 必做 | 前 5 秒有钩子 | 开头迅速建立场景、问题或反差；“有 hook 字段”不等于实际有吸引力 |
| 必做 | 不同公开 Amazon 链接可用 | 不能按两个示例 ASIN 写分支或返回预制结果；需考虑不同品类、页面结构和缺失字段 |
| 必做 | 部署网址 + GitHub 代码 | 对方会实际测试部署环境，不只是看录屏；录屏可选 |
| 加分 | 质量检查、减少错误与夸大 | 可落实为证据引用、数字一致性、长度检查与无法支持的主张提醒 |
| 加分 | 结合商品图片 | 不是只展示图片；应分析可见特征，并说明图片不能证明材料、性能等隐藏属性 |
| 自主建议 | 来源时间、状态分类、复制按钮、响应式布局 | 有助可用性，但不能宣称这是题目逐条规定的内容 |

原材料只写笔试预计半小时至一小时，不能把这个估计当作已经承诺的完整开发工期。截止写“本周日”，绝对日期尚未核实。面试仅明确为一小时以内、共享屏幕的 vibe coding，未给具体面试题。

### 2.2 工具使用者与文案受众不同

合理的产品假设是：工具使用者为需要快速理解商品的团队成员，文案受众为潜在消费者。因此页面应兼顾两件事：

- 对操作者：信息是否正确、分析为什么成立、哪里存在不确定性。
- 对消费者：使用场景是否具体、开头是否值得继续听、语言是否自然，而非一串参数。

建议分析采用“**页面特征 → 用户收益 → 使用场景/人群 → 购买顾虑或适用限制**”结构。例如页面明确说“可折叠”，可以推断其适合重视收纳的人；不能进一步声称“超轻”“一秒收纳”“可随身登机”，除非分别有依据。

### 2.3 三种信息状态

1. **页面事实**：页面或数据服务实际提供的名称、规格、标称功能等，保留来源。页面标称本身不等于独立实验验证的性能。
2. **分析推断**：基于特征推导的用户、场景、痛点和收益，明确标记为推断并关联依据。
3. **未知**：未取得的价格、规格、评论、销量等，保持未知；不能用 `0`、默认五星或模型常识补齐。

价格缺失不必使整个商品分析失败；但只有标题、验证码页或搜索摘要时，也不能假装已有充分的商品详情。应先判断材料是否足以支持有意义的分析。

### 2.4 真正容易失败的地方

- HTTP 200 也可能是验证码、登录提示、配送地址选择或无关页面。
- 同一商品会有站点、币种、卖家、变体及配送地区差异；不能把父 ASIN、其他规格和当前报价混在一起。
- DOM 选择器有多个命中，不等于取得了正确价格；当前价、划线价、分期价和优惠券不是同一概念。
- 模型不能仅凭 URL 自动获得实时商品内容。抓取失败时把链接直接交给模型，不是合格兜底。
- 提示词里的“不要幻觉”“严格 150 字”不代替验证程序。
- 过度堆栈会挤占题目真正关注的产品分析、文案效果和失败体验。

## 3. 调研方法与可复查快照

筛选标准：与单品工作流的贴合程度、源码是否可解释、外部依赖、失败处理、事实性边界、许可证和裁剪成本。未用 star 数代替质量判断，也没有做性能排名。

参考源码保存在工作区 `/data/workspace/remote_test/references/`，**不在本项目 Git 仓库内，不是业务依赖或子模块**。全部为默认分支的浅克隆，保留供后续阅读。以下日期是快照提交时间，不代表最新发布版本或活跃度保证。

| 项目 | 本地子目录 | 审阅快照 | 快照提交日期 | 主要参考价值 |
| --- | --- | --- | --- | --- |
| [Bright Data Amazon Product Analyzer](https://github.com/brightdata/amazon-product-analyzer) | `amazon-product-analyzer/` | `c6b198dfde9c5d7a91323bed8d4d508eced6dd09` | 2025-10-08 | 数据服务接入、规范化、分析 UI 的分层 |
| [Pricewise](https://github.com/adrianhajdin/pricewise) | `pricewise/` | `94b72079a2f7928d194087df60e9fb1c04446636` | 2023-12-30 | 商品链接输入、商品卡片及 DOM 提取思路 |
| [OpenAI Structured Outputs Samples](https://github.com/openai/openai-structured-outputs-samples) | `openai-structured-outputs-samples/` | `7f4e3446ac9a081b2686314d028ce539b6f286f9` | 2025-12-15 | 服务端 schema 约束与结构化结果展示 |
| [Crawl4AI](https://github.com/unclecode/crawl4ai) | `crawl4ai/` | `862f6bccb9c063f49b9d42701baa0eea17a4993f` | 2026-08-31 | 采集、清洗、Markdown 与提取策略解耦 |
| [LLM Scraper](https://github.com/mishushakov/llm-scraper) | `llm-scraper/` | `2b43d999a17eac040f7cc315fc21dd526870564f` | 2026-06-15 | 更轻量的 TypeScript + AI SDK 结构化提取 |

另核对了 Firecrawl、ScrapeGraphAI 和 Amazon Creators API 的官方资料，未将这些文档核查冒充完整源码审阅或服务实测。当前五份克隆总占用约 82 MiB，核查时工作树均干净。

## 4. 五个项目的源码级评估

本节文件路径相对于各参考仓库；代码摘录路径相对于父工作区，均对应上表固定快照。

### 4.1 Bright Data Amazon Product Analyzer：贴近业务，但不是单品助手

**真实流程**：Streamlit 选择市场并输入关键词 → Bright Data 触发搜索任务 → 轮询快照 → 下载多商品结果 → Pandas 规范化和规则分析 → 图表/推荐；用户另外提问时才进入 Gemini。不是输入指定商品 URL 后自动生成三类输出。

值得借鉴：

- `amazon_analytics/api.py` 的 `BrightDataAPI` 将任务触发、状态查询、结果下载拆开。
- `amazon_analytics/data_processor.py` 将数据清洗与界面分离。
- `streamlit_app.py` 有加载状态、商品目录和后续问答，适合观察分析工具的信息组织。

不能照搬的关键点：

| 源码位置 | 静态发现 | 对本题的影响 |
| --- | --- | --- |
| `BrightDataAPI.trigger_search`，`streamlit_app.py` 搜索分支 | 输入是 `keyword`，URL 来自所选市场站点；提交含搜索页数 | 不能把关键词搜索当成指定单品抓取；应另核单商品接口契约 |
| `streamlit_app.py` 的 `smart_wait_for_results` | UI 实际走 `while True` 轮询，无总截止；客户端另有带截止的等待函数但该 UI 不用 | 外层必须控制总等待，不能只给单个 HTTP 请求设超时 |
| `GeminiAIEngine.query_with_data` | 成功响应固定写可信度与核验通过 | 是直接可见的反例，不能用它作为“内容已核验”的实现 |
| `DataProcessor.process_raw_data` 与 `_create_anti_hallucination_prompt` | 规范化字段与 Prompt 所读字段存在错配；部分未知数字填零 | 会把未知误传成事实，需统一数据契约 |
| `requirements.txt`、`config.py` | 依赖 Streamlit、Pandas、Plotly、Requests、Tenacity、Google GenAI 及 Bright Data 服务 | 比我们的单品流程更重，且调用外部服务不等于免费 |

固定核验状态的直接证据：

```270:274:references/amazon-product-analyzer/amazon_analytics/gemini_ai_engine.py
                products_analyzed=len(df),
                data_source='session_data',
                verification_passed=True,
                fact_check_score=0.99,
                computed_facts=computed_facts,
```

同一响应还将 `confidence` 固定为 `0.99`。其 Prompt 虽要求引用 ASIN，却没有逐条主张到原始字段的核验，也没有本题的中文、150 字或钩子检查。

**许可**：当前快照未发现 `LICENSE`、`LICENCE` 或 `COPYING` 正文，仅见 README 的 MIT 徽章，不能据此当作已完成许可核验。建议学习分层，不直接搬用源码，除非补充确认授权。

**结论**：领域相关性高，适合借鉴流程和识别陷阱；不推荐整体作为本题底座。

### 4.2 Pricewise：UI/商品链接流程可参考，数据默认值必须重写

**真实流程**：`components/Searchbar.tsx` → `lib/actions/index.ts` → `lib/scraper/index.ts` → MongoDB 价格历史 → 产品展示。主要解决追价，不提供 LLM 产品分析或短视频文案。

可借鉴：输入框、加载反馈、产品图与信息卡片、服务端抓取边界，以及 `#productTitle`、主图属性等候选定位方式。

重要问题：

- 前端使用 `hostname.includes` 等宽泛规则判断 Amazon，服务端 action 仅判空。公开入口必须在服务端重新验证 URL，不能信任客户端。
- scraper 的商品对象包含写死的品类、评论数与评分：

```71:73:references/pricewise/lib/scraper/index.ts
      category: 'category',
      reviewsCount:100,
      stars: 4.5,
```

- 缺失币种回退 `$`；价格用数字转换和 `||` 回退，无法忠实表达未知。`lib/utils.ts` 的价格清洗只保留数字与点，不能当成所有站点的通用解析。
- 描述提取会收集宽泛列表节点，可能混入页面其他内容；图片展示也不等于多模态分析。
- `axios.get` 没有显式请求超时；scraper 异常仅输出控制台，action 收到空值可直接结束，UI 不一定显示错误原因。
- 代码声明了 Bright Data 相关选项，但静态看到选项不证明代理链路已正确配置或能成功抓取。
- `package.json` 使用部分旧依赖基线，并将 Next/React/TypeScript 写为 `latest`。不能把 manifest 字符串当成已验证的安装组合，也不应原样继承。
- 追价数据库、邮件通知和周期任务不是本题必需能力，应剥离。

**许可**：当前快照未发现许可正文；`private: true` 是包配置，不是复用授权。

**结论**：看交互与选择器思路，不直接复制整套业务，更不能保留样例默认数据。

### 4.3 OpenAI Structured Outputs Samples：优先参考输出契约，不搬 PDF 业务

重点审阅 `resume-extraction`，它展示“已有文本 → 服务端模型调用 → 按 schema 输出 → 界面展示”。不是 Amazon 采集项目。

- `lib/resume.ts` 用 Zod 表达结构，`lib/constants.ts` 要求无法识别的字段留空。
- `app/api/extract_resume/route.ts` 使用服务端 SDK，schema 进入 `response_format`，不是仅写在自然语言提示词中：

```15:21:references/openai-structured-outputs-samples/resume-extraction/app/api/extract_resume/route.ts
  const stream = openai.beta.chat.completions.stream({
    model: MODEL,
    messages: [
      { role: 'system', content: EXTRACT_VALUES_PROMPT },
      { role: 'user', content: resumeText }
    ],
    response_format: zodResponseFormat(ResumeSchema, 'event')
```

- 该示例的实际 `MODEL` 为 `gpt-4o-mini`；不能根据整个仓库最新提交标题推断每个子应用都使用同一个新模型。
- 服务端输出换行分隔的 JSON 增量，客户端按行合并；不是完整 SSE 协议。增量对象应展示为草稿，而非最终通过校验的成品。
- `app/page.tsx` 缺少完整的 HTTP 状态/错误提示衔接，`TextDecoder` 未启用流式解码，中文跨块需额外处理；没有字数或证据一致性门禁。
- PDF 入口使用固定 `/tmp/resume.pdf` 并打印原始文本，不适合直接继承到公开的并发产品工具。我们不需要 PDF 上传流程。
- 所查示例声明 Next `^14.2.23`、OpenAI SDK `^4.61.0`、Zod `^3.23.8`，是历史 manifest，不代表本项目应使用的当前版本。

**许可**：已核 `LICENSE` 为 MIT，实质复制时需保留相应版权与许可通知。

**结论**：适合借“结构化契约 + 服务端调用”的模式；重新实现简单业务路由，比改造整套简历工具更直接。

### 4.4 Crawl4AI：采集基础设施参考，不是事实正确性保证

`crawl4ai/async_webcrawler.py` 中的主链先取得 HTML，再进入清洗、Markdown 与可选提取策略；结果保留多种表示。可借鉴**采集与提取分层**，也可将其作为未来自托管路线的候选。

但有几个必须看源码才能确认的边界：

1. `LLMExtractionStrategy` 会将 schema 写入 Prompt，并解析返回 JSON；所查路径没有按传入 schema 执行业务字段验证。不能把这个接口名称直接等同于模型端严格结构化输出。
2. 爬取 `success` 与有效商品内容、提取正确性不是同一概念。
3. 所查 LLM 空响应分支先标记错误，后续循环又覆盖错误标志：

```922:925:references/crawl4ai/crawl4ai/extraction_strategy.py
                if not content:
                    finish_reason = getattr(response.choices[0], "finish_reason", "unknown")
                    blocks = [{"index": 0, "error": True, "tags": ["error"],
                               "content": f"LLM returned no content (finish_reason: {finish_reason})"}]
```

```939:940:references/crawl4ai/crawl4ai/extraction_strategy.py
                for block in blocks:
                    block["error"] = False
```

这是给定快照的静态控制流问题，未运行复现，也不代表已评价全库。应用仍应独立检查最终内容，不能只信一个布尔状态。

4. `pyproject.toml` 声明 Python ≥3.10、Playwright、Patchright 等依赖。Torch/Transformers 是可选项，本题不需要默认启用；浏览器和额外运行时会增加部署负担。
5. LLM 分块路径使用并发聚合，应用需独立控制页面大小、分块数、并发和模型预算。
6. 通用接口支持网页之外的输入形态，不能直接原样暴露给用户输入；要有服务端输入与网络访问边界。
7. 通用爬虫能力不证明特定 Amazon 商品当前可访问，也不自动解决变体、币种、功效证据和中文口播。

**许可**：`LICENSE` 先列 Apache 2.0 条款，之后另附 `Attribution Requirement`，要求指定署名；README 的“recommended”与“must include”表述也不完全一致。复用前必须核对这些额外文字并安排署名，不能只依据仓库的 Apache 标签。

**结论**：值得学习，当前小工具未必需要引入；不建议为了单页任务搬整个 Docker 服务或全部可选依赖。

### 4.5 LLM Scraper：较贴合 TypeScript 方案的轻量模块参考

这是本轮补充筛选到的较小模块。`LLMScraper` 接收调用方创建的 Playwright `Page`，然后预处理并调用模型；浏览器启动、导航和关闭由宿主负责。

- `examples/streaming.ts` 用 Zod schema 和 `Output.object`，显式选择 HTML 输入。
- `src/models.ts` 的数据抽取分支使用 AI SDK 6 的 `generateText/streamText` 配合 `output`，不是只把 JSON 格式要求塞进 Prompt：

```51:57:references/llm-scraper/src/models.ts
  const result = await generateText({
    model,
    output,
    system,
    messages,
    ...rest,
  })
```

- `run` 返回 SDK 的最终 `result.output`；`stream` 包装层只暴露 `partialOutputStream`，不是完整业务验收出口。最终对象仍需我们校验。
- 附带页面 URL，不等于拥有每个字段的原文、DOM 定位、采集时间和可信度证据。
- 所查入口、模型与示例没有完整的 Amazon URL 校验、受限页面识别或事实核验策略；不能把浏览器和 SDK 的默认行为当成产品级兜底。
- `generate` 是生成提取代码的另一条路径，不是本题必须能力；不建议生成并自动执行代码来扩大范围。
- `package.json` 标为 `2.0.0`，声明 `ai ^6.0.77`；示例相关的 Playwright `^1.58.2`、Zod `^4.3.6`、OpenAI provider `^3.0.26` 位于开发依赖。宿主仍需正确选择并锁定自己的兼容组合。

**许可**：已核 `LICENSE.md` 为 MIT。

**结论**：若最终选择 TypeScript + 自己管理浏览器，这是比大型图式框架更贴近需求的参考；若采集服务已提供可靠结构化字段，可以直接借其输出模式，而不必再增加这个依赖。

## 5. 商品信息获取路线比较

先解决“能否取得指定商品材料”，再选择如何分析。以下是候选路线，不是本轮实测排名。

| 路线 | 有利点 | 主要限制 | 本题建议 |
| --- | --- | --- | --- |
| Amazon 专用商品数据服务 | 可能直接返回 ASIN、价格、规格、图片等，减少 DOM 适配 | 费用、账户、支持站点、字段覆盖、时效和任务延迟均需实测；搜索接口不是详情接口 | 若预算与接入条件允许，优先做小样本详情获取验证；供应商不预设 |
| Firecrawl 托管单页抓取/JSON 模式 | 单 URL 到 Markdown/JSON 的接入较薄，可与 TS 服务端结合 | 通用抓取不保证 Amazon 成功；LLM 提取仍会有缺失与错误；需要核对用量与服务条款 | 可作为单页候选，不直接当真实性担保 |
| HTTP + HTML/JSON-LD/DOM 解析 | 依赖少，证据定位清楚；原生解析无额外提取模型调用 | 地区模板、动态渲染及访问限制；JSON-LD 不保证存在或完整 | 可做轻量适配；失败必须显式，不承诺仅此覆盖所有页面 |
| Playwright + LLM Scraper/直接结构化生成 | 同一 TS 栈；可处理渲染后的页面内容 | 浏览器资源、沙箱、网络边界、部署体积；模型结构正确不等于事实正确 | 仅在需要渲染且运行环境允许时采用，不能自动解决访问限制 |
| Crawl4AI | 清洗、Markdown、缓存、提取策略比较齐全 | Python/浏览器依赖、额外署名、通用框架裁剪成本 | Python 熟悉度较高或后续扩展采集时再考虑 |
| ScrapeGraphAI | 官方文档提供单页 `SmartScraperGraph` 与多模型图式流程 | 本地库需自己管理 Playwright/LLM；图式封装并不解决商品证据 | 本轮仅文档评估，对单页短流程没有必须引入它的理由 |
| Amazon Creators API | 官方商品目录接口路线 | 需按官方流程注册并核对适用资格、用途与字段条件；不能默认临时即可接入 | 如已有可用资格可评估；不将旧 PA-API 教程作为默认起点 |
| 用户粘贴详情文本 | 获取受限时仍能演示分析能力 | **不能替代原题“输入链接自动分析”的主链** | 仅可作为明确标记的辅助入口，不用它掩盖链接成功率 |

### 5.1 Firecrawl 的两个易混点

官方单页 JSON 模式说明：v2 在 `formats` 的 JSON 对象中带 schema，不能套用旧版 `jsonOptions`。文档还指出 JSON 提取基于 Markdown，原 HTML 属性可能已丢失；需要主图属性等证据时，要保留可解析的原始材料，而不是仅依赖模型转换后的 JSON。[资料 D1]

开源自托管与 Cloud 不完全等价。官方文档明确区分基础服务、Cloud 扩展能力以及运维责任；克隆仓库并不自动获得托管端全部能力。[资料 D2]

Firecrawl 核心 `LICENSE` 是 AGPLv3。调用托管 HTTP API、使用某个 SDK、复制/修改核心源码是不同的许可与服务关系，应分别核对；不能简单推断整个应用都会适用同一种许可。[资料 D3]

### 5.2 Amazon 官方接口的当前状态

本轮读取的官方公告明确：PA-API 5 已弃用，由 Creators API 接替；继续调用旧接口可能收到公告所列的 403/AccessDenied 错误。迁移文档要求新的注册与接入方式，旧接入资料不能直接沿用。[资料 D4、D5]

公告本页没有给出明确弃用日期，因此不引用检索结果中的二手日期。Creators API 具体资格条件本轮未完整核验，不编造“任何账号可用”或具体销量门槛。

所有路线都应遵守来源与供应商允许的访问、使用和保存条件；遇到登录、验证码或访问限制时返回可理解状态，不设计规避限制的流程。

## 6. 推荐的最小架构

### 6.1 原则：单页、单业务流程、可替换采集源

建议顺序：**URL 校验 → 商品材料获取 → 事实规范化 → 一次分析与文案生成 → 规则校验 → 页面展示**。

| 模块 | 最小职责 | 不承担的职责 |
| --- | --- | --- |
| 页面 | 输入、状态、三块结果、复制、来源/风险提示 | 不持有供应商密钥，不直接访问 Amazon |
| 服务端入口 | 校验请求、总超时、限流/重复请求控制、错误映射 | 不把网页内容当成可执行指令 |
| 采集适配器 | 用支持的来源取得指定商品材料，返回来源、时间和访问状态 | 不编造详情、不用搜索相似结果替换指定商品 |
| 事实规范化 | 提取字段、保留证据、区分缺失和冲突 | 不输出营销结论 |
| 分析/文案生成 | 基于事实生成带依据的分析与中文口播 | 不凭常识补新规格、销量或功效 |
| 校验器 | 校验结构、引用、数字/单位、长度与风险词；至多一次受控修正 | 不用固定百分比假装真伪鉴定 |

若采集服务已返回足够的规范化事实，一次模型调用可以同时生成分析和口播；无需为了展示“智能体”再拆成多个循环。若输入只有长篇页面材料，先用可控提取获得事实，再做生成，明确增加一次模型调用的成本。

### 6.2 最小数据契约

以下是建议设计，不是已存在代码：

| 对象 | 建议字段 | 约束 |
| --- | --- | --- |
| 来源 | 原链接、规范化链接、ASIN、站点、变体、采集时间、提供方 | 请求对象与输出对象必须对应；保存地区/变体差异 |
| 证据 | `evidence_id`、原文片段、字段/DOM/JSON 定位 | ID 由程序产生；原文确实来自本次材料，不让模型虚构证据 |
| 商品事实 | 名称、品类、价格与币种、功能、规格、图片、缺失字段 | 每个非空重要字段有来源；明确哪些是页面标称 |
| 产品分析 | 人群、场景、痛点、卖点、限制/顾虑 | 标注推断；主张关联有效证据 ID；不给无依据的销量/好评概括 |
| 口播 | 钩子、正文、最终合并文本、关联证据 | 输出中文；最终复制内容受 150 字约束 |
| 质量结果 | 字数、结构是否通过、证据是否存在、数字检查、风险提醒 | 展示实际检查项目与未检查项，不显示虚构“99%准确” |

原文存在性可由程序检查；“这段原文是否真的支持这个主张”仍涉及语义判断。规则检查与第二次模型复核都不能当成绝对真伪保证，页面应保留人工可核对的证据。

### 6.3 URL 与公开服务边界

- 使用标准 URL 解析，服务端验证协议、支持站点的精确主机名及商品路径；不使用字符串包含关系判断域名。
- 支持常见的 `/dp/ASIN`、`/gp/product/ASIN` 等路径形式，去掉不必要的跟踪参数，同时保留影响站点/变体的语义。不要仅用 ASIN 跨站点混合结果。
- 两个题目示例都来自 Amazon.com，但原题没有承诺测试只限 `.com`。应把支持站点做成明确配置并在页面说明，不宣称已经覆盖全部市场。
- 如支持短链，对每次重定向重新校验；不支持时明确提示。浏览器路线还需关注子资源请求及网络出口限制。
- 禁止访问本地文件、内网地址或任意协议；地址解析/连接及重定向均需防护，不能只检查最初 URL 字符串。
- 页面文本与图片是外部数据，不能改变系统约束、请求密钥或触发任意工具操作；前端不直接渲染未净化的原始 HTML。
- 控制请求体、响应体、页面文本、图片数量、超时、重试和并发；模型/API 费用在服务端设上限。

这是公开 URL 工具的基本工程边界，不代表需要为笔试搭建复杂安全平台。

## 7. 产品分析与口播怎样做到有质量

### 7.1 分析避免模板填空

建议每条卖点至少能回答：

- 对应的页面依据是什么？
- 给用户带来的实际收益是什么？
- 在什么场景下有价值？
- 哪些条件未验证，哪些用户未必适用？

“目标用户：所有人；使用场景：日常；核心卖点：高品质、高性价比”没有产品理解。缺少竞品比较时，也不要宣称“同价位最好”或“性价比第一”。

### 7.2 150 字与前 5 秒应分别验收

建议保守计数规则：对最终合并口播去除首尾空白后按 Unicode 码点计数，汉字、字母、数字、标点及内部空白都计入，总数 ≤150。展示标签与证据列表不复制进正文。此规则是我们拟定的工程标准，不是原题额外给出的算法。

钩子放在第一句，建议以场景提问、真实烦恼或可证明的反差开头，避免长品牌名、问候和参数罗列。可先按约 3–4 字/秒估计 15–20 字的首句，再实际朗读确认；字符数不能保证 5 秒时长，更不能证明有吸引力。

超长时优先只重写文案并重新校验，不重新抓取商品；不能机械截断导致半句、遗漏限定词或改变含义。

**说明性示例，不是对题目示例商品的实测输出：**假设已取得“可折叠”“带杯架”两条页面证据，文案可以围绕“露营坐下才发现，饮料没地方放？”切入，再讲杯架的取放便利与折叠收纳。不能顺带添加“承重 150 公斤”“防水”“最低价”等未取得的主张。

### 7.3 建议优先做的加分项

1. **质量卡片**：显示实际字数、缺失信息、哪些主张来自页面、哪些是推断。
2. **证据可展开**：点击卖点能看到对应原文和来源。
3. **仅重新生成口播**：事实不变时不重复采集，避免无谓延迟与费用。
4. **图片理解后置**：只描述可见外观/结构；图片观察与文字标称分开，不能由外观推出材质、防水等级或认证。

## 8. 后续验证计划与失败标准

以下均为**待执行计划，不是本轮结果**。应在获准实现并具备可用服务后进行，不能把此表写成“测试全部通过”。

### 8.1 样本与检查矩阵

| 测试组 | 建议内容 | 通过标准 |
| --- | --- | --- |
| 主链冒烟 | 先选 3–5 个不同品类的公开商品 | 指定商品身份正确，取得足够材料，能完整输出三类结果 |
| 泛化样本 | 题目两个示例 + 至少 8 个非示例商品，覆盖不少于 5 类；记录失效链接 | 不硬编码；分别报告采集成功、完整交付和错误分类，不混成一个成功率 |
| URL 形态 | 同商品短/长路径、跟踪参数、支持的站点和变体 | 规范化结果正确，不混币种/变体；超出支持范围清楚提示 |
| 事实缺失 | 无价格、无评分、规格不足等受控样例 | 未知保持未知，不补零或固定数值；必要时仅部分结果或停止生成 |
| 获取异常 | 超时、429/5xx、验证码或非商品页面样例 | 等待有截止、重试有上限，不把错误页面送去生成“成功”文案 |
| 模型异常 | 非法结构、拒答、截断、断流、超长文案 | 不把 partial 当最终；适当受控重试，否则给出具体失败阶段 |
| 内容一致性 | 数字/单位、证据 ID、无来源功效/销量主张 | 引用有效，定量信息与材料一致；不支持的主张删去或明确标注 |
| 文案体验 | 中文自然度、场景、首句钩子、朗读时长 | 总长通过；人工能理解商品收益，开头不拖沓 |
| 安全边界 | 非支持主机、非网页协议、重定向、页面注入文本 | 服务端拒绝不合规访问，外部文本不能更改任务边界 |
| 部署验收 | 最终运行环境访问、刷新、连续多次请求 | 不是只在本机可用；密钥不出现在前端或日志，错误可恢复 |

真实 Amazon 页面可能变化，应记录测试时间、站点、输入和采集来源。受控异常测试可使用清楚标记的 fixture，但不得把 fixture 当成真实链接测试或对外演示的自动抓取结果。

### 8.2 应记录的结果

- 每个链接的身份是否一致、材料是否足够、各阶段耗时、失败类别。
- 页面字段覆盖与人工核对结果，而非仅统计 HTTP 200。
- 每条文案字数、无依据主张数、语义核验与朗读结果。
- 模型调用次数、输入/输出 token、采集服务计费单位与实际费用；不在未实测时承诺某个价格或 P95 延迟。
- 辅助粘贴文本入口的完成率与主 URL 链路分开，不能用辅助入口掩盖主链问题。

一次请求成本可按“采集 + 必要提取 + 分析/生成 + 有上限的修正”拆开记录。没有必要先设计永久历史存储来完成这些基本验证。

## 9. 实施顺序与取舍建议

1. **先做来源可行性门槛**：确认支持站点、3–5 个真实链接的详情字段、失败行为、费用和许可。若来源拿不到有效内容，先解决来源，不继续美化“空转”页面。
2. **定义最小事实与输出 schema**：字段允许未知，固定事实/推断边界，明确字数计算方式。
3. **实现最小端到端网页**：一条采集路线、一个模型供应商、一个服务端业务入口，展示完整状态与三类输出。
4. **补确定性质量检查**：来源引用、数字/单位、长度；最多一次针对性修正，避免无限重试。
5. **用非示例商品验证并改进文案**：特别检查多品类，不只拿两个题目链接调 Prompt。
6. **再考虑图片、细节交互和交付材料**：没有通过主链验证前，不扩展成价格监控或复杂 Agent 系统。

待真正开始实现时，需要确定的少数事项是：开发者更熟悉 TS 还是 Python、可用的商品信息来源和模型、预算与最终运行环境。部署平台届时再选，本调研不创建任何远程资源。

面试准备应围绕可解释性：为什么选择这个信息来源、为什么 schema 不等于事实核验、如何处理未知、150 字怎样保证、换一个链接失败时怎么定位。相比完整搬用大型模板，这些取舍更容易现场说明和修改。

## 10. 许可、复用方式与本轮边界

| 对象 | 本轮核查 | 建议复用方式 |
| --- | --- | --- |
| Bright Data 示例 | 未见许可正文，README 有 MIT 徽章 | 学习流程；实质复制前补核授权 |
| Pricewise | 未见许可正文 | 学习 UI/数据流/问题；不默认可以整库派生 |
| OpenAI 示例 | MIT 正文已读 | 可在遵守通知要求前提下借鉴少量模块 |
| LLM Scraper | MIT 正文已读 | 可评估依赖或模块复用，保留相应通知 |
| Crawl4AI | Apache 标识之外还有附加署名文字 | 先核对具体条款与署名位置，不简化为普通 Apache 标签 |
| Firecrawl 核心 | 在线核对 AGPLv3 许可标题 | 核心复制、自托管改造、SDK 使用、托管服务调用分别评估 |
| ScrapeGraphAI | 官方页面显示 MIT；仅做文档核查 | 如果拟复用，再对具体快照和许可正文核验 |

以上是源码与文档观察，不替代对具体复用方式的许可审查。引用项目应清楚说明借鉴部分与自行实现部分，不把参考项目整体包装成自己的原创成果。

**本轮已做**：仓库首次 README 推送、需求拆解、公开资料检索、五库浅克隆与核心源码审阅、本调研文档。

**本轮未做**：参考项目安装/运行、商品抓取实测、模型调用、浏览器测试、性能/费用测量、应用开发、服务部署、调研文档提交或推送。文档中“可借鉴”“推荐”“测试计划”均不是完成这些事项的声明。

## 11. 来源与继续阅读入口

### 固定快照源码

- S1：[Bright Data 示例快照](https://github.com/brightdata/amazon-product-analyzer/tree/c6b198dfde9c5d7a91323bed8d4d508eced6dd09)。优先读 `amazon_analytics/api.py`、`data_processor.py`、`gemini_ai_engine.py` 与 `streamlit_app.py`。
- S2：[Pricewise 快照](https://github.com/adrianhajdin/pricewise/tree/94b72079a2f7928d194087df60e9fb1c04446636)。优先读 `components/Searchbar.tsx`、`lib/actions/index.ts`、`lib/scraper/index.ts`、`lib/utils.ts`。
- S3：[OpenAI 示例快照](https://github.com/openai/openai-structured-outputs-samples/tree/7f4e3446ac9a081b2686314d028ce539b6f286f9)。优先读 `resume-extraction/lib/resume.ts`、`lib/constants.ts`、`app/api/extract_resume/route.ts`、`app/page.tsx`；这里后三条同样相对于 `resume-extraction/`。
- S4：[Crawl4AI 快照](https://github.com/unclecode/crawl4ai/tree/862f6bccb9c063f49b9d42701baa0eea17a4993f)。优先读 `crawl4ai/async_webcrawler.py`、`crawl4ai/extraction_strategy.py`、`pyproject.toml`、`LICENSE`。
- S5：[LLM Scraper 快照](https://github.com/mishushakov/llm-scraper/tree/2b43d999a17eac040f7cc315fc21dd526870564f)。优先读 `src/index.ts`、`src/models.ts`、`examples/streaming.ts`、`package.json`、`LICENSE.md`。

### 在线官方资料（本轮读取，后续可能变化）

- D1：[Firecrawl 单页 JSON 模式](https://docs.firecrawl.dev/features/llm-extract)。接口版本、输入材料边界、缺失值处理。
- D2：[Firecrawl Open Source vs Cloud](https://docs.firecrawl.dev/contributing/open-source-or-cloud)。自托管与托管能力/运维责任区别。
- D3：[Firecrawl 核心 LICENSE](https://raw.githubusercontent.com/firecrawl/firecrawl/main/LICENSE)。本轮读取标题为 GNU Affero General Public License v3；未克隆固定快照。
- D4：[Amazon PA-API 5 官方弃用公告](https://affiliate-program.amazon.com/creatorsapi/docs/en-us/paapiv5-deprecation)。旧文档已重定向到该公告。
- D5：[Amazon Creators API 官方迁移说明](https://affiliate-program.amazon.com/creatorsapi/docs/en-us/migrating-to-creatorsapi-from-paapi)。接入流程与接口差异。
- D6：[ScrapeGraphAI 官方仓库说明](https://github.com/ScrapeGraphAI/Scrapegraph-ai)。单页图式流程、本地库与托管产品差异；本轮未克隆审阅。

检索局限：GitHub 公共元信息 API 曾限流，个别官方页面抓取失败；上述结论使用实际读到的官方资料或固定源码，不以检索摘要替代事实，也不声称穷尽了所有项目。
